import * as types from '../constants/authTypes';

const initialState = {
    userId: null
}

export default (state=initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}